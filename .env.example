# ======================================
# Environment Variables Configuration
# ======================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control

# ======================================
# Database Configuration
# ======================================
# For local development, use a local SQLite database
DATABASE_URL="file:local.db"

# For production with Turso (LibSQL Cloud), use a Turso database URL
# DATABASE_URL="libsql://your-database-name.turso.io"
# DATABASE_AUTH_TOKEN="your-turso-auth-token"

# ======================================
# Authentication (better-auth)
# ======================================
# Generate a secure random secret (min 32 characters)
# Run: openssl rand -base64 32
BETTER_AUTH_SECRET="development-secret-change-in-production-min-32-chars"

# ======================================
# Custom OAuth IDP Configuration
# ======================================
# Set to "true" to enable OAuth authentication with your custom IDP
OAUTH_IDP_ENABLED="false"

# Display name for your IDP (shown on login button)
OAUTH_IDP_NAME="Custom IDP"

# OAuth client credentials from your IDP
OAUTH_IDP_CLIENT_ID="your-client-id-here"
OAUTH_IDP_CLIENT_SECRET="your-client-secret-here"

# OAuth endpoints from your IDP
# Example for Okta: https://your-domain.okta.com/oauth2/default/v1/authorize
OAUTH_IDP_AUTHORIZATION_ENDPOINT="https://your-idp.example.com/oauth/authorize"

# Example for Okta: https://your-domain.okta.com/oauth2/default/v1/token
OAUTH_IDP_TOKEN_ENDPOINT="https://your-idp.example.com/oauth/token"

# Example for Okta: https://your-domain.okta.com/oauth2/default/v1/userinfo
OAUTH_IDP_USERINFO_ENDPOINT="https://your-idp.example.com/oauth/userinfo"

# Space-separated list of OAuth scopes to request
# Common scopes: openid profile email
OAUTH_IDP_SCOPES="openid profile email"

# ======================================
# Optional: Built-in Social Auth Providers
# ======================================
# Uncomment and configure if you want to use GitHub OAuth
# GITHUB_CLIENT_ID="your-github-client-id"
# GITHUB_CLIENT_SECRET="your-github-client-secret"

# Uncomment and configure if you want to use Google OAuth
# GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ======================================
# AI Provider API Keys (Optional)
# ======================================
# At least one is recommended if using AI features

# OpenAI API Key (for GPT models)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY="sk-proj-..."

# Anthropic API Key (for Claude models)
# Get from: https://console.anthropic.com/
# ANTHROPIC_API_KEY="sk-ant-..."

# Google AI API Key (for Gemini models)
# Get from: https://aistudio.google.com/app/apikey
# GOOGLE_API_KEY="..."

# ======================================
# Application Configuration
# ======================================
# Node environment
NODE_ENV="development"

# ======================================
# Client-Safe Variables (NEXT_PUBLIC_ prefix)
# ======================================
# These variables are exposed to the browser
# Never put secrets here!

# Application URL (used for OAuth redirects)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Set to "true" to enable OAuth IDP on the client (must match OAUTH_IDP_ENABLED)
NEXT_PUBLIC_OAUTH_IDP_ENABLED="false"

# Display name for OAuth IDP (must match OAUTH_IDP_NAME)
NEXT_PUBLIC_OAUTH_IDP_NAME="Custom IDP"

# ======================================
# Setup Instructions
# ======================================
# 1. Copy this file to .env.local
# 2. Fill in your actual values (never use these placeholder values in production)
# 3. For local development, the default DATABASE_URL and BETTER_AUTH_SECRET are sufficient
# 4. For OAuth IDP setup:
#    - Set OAUTH_IDP_ENABLED="true"
#    - Fill in your IDP credentials and endpoints
#    - Set the corresponding NEXT_PUBLIC_ variables
#    - Configure your IDP to redirect to: ${NEXT_PUBLIC_APP_URL}/api/auth/callback/custom-idp
# 5. Never commit .env.local to version control
