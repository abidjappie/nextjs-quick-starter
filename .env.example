# ======================================
# Environment Variables Configuration
# ======================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control

# ======================================
# Database Configuration
# ======================================
# For local development, use a local SQLite database
DATABASE_URL="file:local.db"

# For production with Turso (LibSQL Cloud), use a Turso database URL
# DATABASE_URL="libsql://your-database-name.turso.io"
# DATABASE_AUTH_TOKEN="your-turso-auth-token"

# ======================================
# Authentication (better-auth)
# ======================================
# Generate a secure random secret (min 32 characters)
# Run: openssl rand -base64 32
BETTER_AUTH_SECRET="development-secret-change-in-production-min-32-chars"

# ======================================
# Global Admin Configuration
# ======================================
# Required for accessing the /system admin panel
# The global admin can manage OAuth providers and system settings
GLOBAL_ADMIN_EMAIL="admin@example.com"
GLOBAL_ADMIN_PASSWORD="admin123"

# IMPORTANT: Change these credentials in production!
# After setting these values:
# 1. Run: pnpm db:seed
# 2. Sign in with these credentials at /login
# 3. Access the admin panel at /system

# ======================================
# OAuth Provider Management
# ======================================
# OAuth providers are now managed through the /system admin panel
# No environment variables needed - configure providers via web UI
# See OAUTH_SETUP.md for detailed instructions

# ======================================
# Optional: Built-in Social Auth Providers
# ======================================
# Uncomment and configure if you want to use GitHub OAuth
# GITHUB_CLIENT_ID="your-github-client-id"
# GITHUB_CLIENT_SECRET="your-github-client-secret"

# Uncomment and configure if you want to use Google OAuth
# GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ======================================
# AI Provider API Keys (Optional)
# ======================================
# At least one is recommended if using AI features

# OpenAI API Key (for GPT models)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY="sk-proj-..."

# Anthropic API Key (for Claude models)
# Get from: https://console.anthropic.com/
# ANTHROPIC_API_KEY="sk-ant-..."

# Google AI API Key (for Gemini models)
# Get from: https://aistudio.google.com/app/apikey
# GOOGLE_API_KEY="..."

# ======================================
# Application Configuration
# ======================================
# Node environment
NODE_ENV="development"

# Application URL (used for OAuth redirects)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ======================================
# Setup Instructions
# ======================================
# 1. Copy this file to .env.local
# 2. Fill in your actual values (never use these placeholder values in production)
# 3. For local development, the default DATABASE_URL and BETTER_AUTH_SECRET are sufficient
# 4. Set up global admin:
#    - Update GLOBAL_ADMIN_EMAIL and GLOBAL_ADMIN_PASSWORD
#    - Run: pnpm install
#    - Run: pnpm db:seed
# 5. Configure OAuth providers:
#    - Sign in with admin credentials
#    - Go to /system
#    - Add OAuth providers via the web interface
#    - Restart the application for changes to take effect
# 6. Never commit .env.local to version control
